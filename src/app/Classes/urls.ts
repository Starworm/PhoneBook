export class URLs {

  static urlBase = 'https://contacts-telran.herokuapp.com/api';

  static urlLogin = URLs.urlBase + '/login';
  static urlRegister = URLs.urlBase + '/registration';
  static urlContacts = URLs.urlBase + '/contact';
  static urlAdd = URLs.urlBase + '/contact';
  static urlCorrect = URLs.urlBase + '/contact';
  static urlRemove = URLs.urlBase + '/contact';

}
